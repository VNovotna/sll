This project needs Docker and Docker compose.

Run it with:
`docker-compose run --rm console`

Or if you hate Docker run it on any PHP 8.2 enabled machine:
`php src/console.php`

Note: running `composer install` would be needed on first run.
